<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Search by Name</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        /* Hide the Search Results section initially */
        #search-results {
            display: none;
        }
    </style>
</head>
<body>
    <div class="container mt-3">
        <div class="container mt-3">
            <h1>Search Trains by Passenger Name</h1>
            <form id="search-form">
                <div class="mb-3 mt-3">
                    <label for="first_name">First Name:</label>
                    <input type="text" class="form-control" id="first_name" placeholder="Enter First Name" required>
                </div>
                <div class="mb-3 mt-3">
                    <label for="last_name">Last Name:</label>
                    <input type="text" class="form-control" id="last_name" placeholder="Enter Last Name" required>
                </div>
                <button id="search-button" type="submit" class="btn btn-primary" value="Search">Search</button>
            </form>
        </div>
        <h1>Search Results</h1>
        <table border="1">
            <tr>
                <th>Train Number</th>
                <th>Train Name</th>
                <th>Source Station</th>
                <th>Destination Station</th>
                <th>Booking Status</th>
            </tr>
            {% for booking in bookings %}
            <tr>
                <td>{{ booking[0] }}</td>
                <td>{{ booking[1] }}</td>
                <td>{{ booking[2] }}</td>
                <td>{{ booking[3] }}</td>
                <td>{{ booking[4] }}</td>
            </tr>
            {% endfor %}
        </table>
        <div id="search-results" class="container mt-3">
            <h1>Search Results</h1>
            <table id="results-table" class="table">
                <thead>
                    <tr>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Train Number</th>
                        <th>Train Name</th>
                        <th>Source Station</th>
                        <th>Destination Station</th>
                        <th>Booking Status</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Results will be dynamically added here -->
                </tbody>
            </table>
        </div>
    </div>

    <!-- <script>
        $(document).ready(function() {
            // Handle form submission
            $("#search-form").submit(function(event) {
                event.preventDefault(); // Prevent default form submission
                search(); // Call search function
            });

            function search() {
                // Get form data
                var formData = {
                    first_name: $("#first_name").val(),
                    last_name: $("#last_name").val()
                };

                // Send AJAX request to Flask route
                $.ajax({
                    type: "POST",
                    url: "/search_by_name",
                    data: formData,
                    dataType: "json",
                    success: function(data) {
                        // Populate table with returned data
                        var tableBody = $("#results-table tbody");
                        tableBody.empty(); // Clear previous results
                        $.each(data, function(index, booking) {
                            var row = "<tr>";
                            row += "<td>" + booking['first_name'] + "</td>";
                            row += "<td>" + booking['last_name'] + "</td>";
                            row += "<td>" + booking['train_number'] + "</td>";
                            row += "<td>" + booking['train_name'] + "</td>";
                            row += "<td>" + booking['source_station'] + "</td>";
                            row += "<td>" + booking['destination_station'] + "</td>";
                            row += "<td>" + booking['booking_status'] + "</td>";
                            row += "</tr>";
                            tableBody.append(row);
                        });
                        $("#search-results").show(); // Show search results
                    },
                    error: function(error) {
                        console.log("Error:", error);
                    }
                });
            }
        });
    </script> -->
</body>
</html>
